{
  "name": "svgp",
  "version": "0.1.0-alpha.1",
  "description": "",
  "keywords": [ "svg", "parse", "parser" ],
  "author": "Kir Belevich <kir@soulshine.in> (https://github.com/deepsweet)",
  "homepage": "https://github.com/svg/svgp",
  "bugs": {
    "url": "https://github.com/svg/svgp/issues"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/svg/svgp.git"
  },
  "main": "./index.js",
  "dependencies": {
    "sax": "0.6.x"
  },
  "devDependencies": {
    "eslint": "0.10.x",
    "jscs": "1.8.x",
    "mocha": "2.0.x",
    "chai": "1.10.x",
    "istanbul": "0.3.x",
    "coveralls": "2.11.x"
  },
  "scripts": {
    "eslint": "eslint *.js lib/*.js test/*.js",
    "jscs": "jscs *.js lib/*.js test/*.js",
    "lint": "npm run eslint && npm run jscs",
    "unit": "mocha --recursive -R spec",
    "test": "npm run lint && npm run unit",
    "coverage": "istanbul cover _mocha --report lcovonly -- -R spec",
    "coveralls": "istanbul cover _mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | coveralls"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "license": "MIT"
}
